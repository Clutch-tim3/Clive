# Plan: Remove Striker Showcase from Homepage

## Goal
Remove the StrikerShowcase component from the landing page HTML so it only appears on the dedicated `/striker` page, while maintaining access via the Nav link.

## Current State
- The homepage (`app/page.tsx`) imports and renders `<StrikerShowcase />` right after the Hero section
- The Nav component has a "Striker" link that points to `/striker`
- There's a dedicated Striker page at `/striker`

## Proposed Changes

### 1. Edit `app/page.tsx`
- Remove the import: `import { StrikerShowcase } from '@/components/home/StrikerShowcase';`
- Remove the `<StrikerShowcase />` component from the JSX

### 2. Verify Nav Link
- Confirm the Nav component still has the "Striker" link pointing to `/striker` (user confirmed this should remain)
- Ensure the dedicated Striker page remains functional

## Implementation Steps
1. Remove StrikerShowcase import from homepage
2. Remove StrikerShowcase component from homepage JSX
3. Test that homepage loads without Striker section
4. Test that `/striker` page still works via Nav link

## Expected Outcome
- Homepage will show Hero → HackKitSpotlight → Products → etc.
- Striker content remains accessible via the "Striker" tab in navigation
- No impact on other functionality

## Questions for User
- Should the "Striker" tab in the Nav remain, or should it be removed as well?
- Are there any other references to StrikerShowcase that need to be considered?