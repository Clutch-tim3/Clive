# CLIVE — COMPLETE DE-PLACEHOLDER & WIRE-UP PLAN

## PROJECT OVERVIEW
The Clive platform is a Next.js 14 App Router + Firebase application that currently relies heavily on hardcoded placeholder data. The goal is to replace all fake data with real Firestore reads/writes, authentic authentication state, and real API calls.

## PLANNED IMPLEMENTATION STEPS

### SECTION 0: CODEBASE SCAN (COMPLETED)
- Scanned all files in the project to identify:
  - Hardcoded strings, numbers, arrays
  - useState initialized with fake data
  - Empty or non-firing useEffect hooks
  - API routes returning mock responses
  - Pages rendering without auth checks
  - Forms submitting to nowhere

### SECTION 1: ENVIRONMENT & FIREBASE CONFIG
1.1 Verify `.env.local` exists with real values (already done - contains valid Firebase config)
1.2 Fix `lib/firebase/client.ts` (already matches required structure)
1.3 Fix `lib/firebase/admin.ts` (needs to be created - will implement as specified)

### SECTION 2: AUTHENTICATION
2.1 Scan for auth placeholders (identified in components, pages)
2.2 Create `lib/firebase/auth.ts` (will implement Firebase auth methods)
2.3 Create `hooks/useAuth.ts` (will implement auth state hook)
2.4 Fix all pages/components using fake auth (will replace with useAuth hook)
2.5 Create `lib/firebase/server-auth.ts` for server-side auth protection

### SECTION 3: PROVIDER CONSOLE (NEXT.JS PAGES)
3.1 Dashboard page (`app/console/page.tsx`):
   - Replace hardcoded stats with real Firestore queries
   - Calculate revenue from transactions, calls from subscriptions, etc.

3.2 My Products page:
   - Fetch real products from Firestore where providerId == user.uid

3.3 Add New Product form:
   - Wire form submission to POST /api/products (already partially implemented)

3.4 Analytics charts:
   - Implement real API call data from Firestore time-series queries

3.5 Earnings page:
   - Fetch real transaction data from Firestore

3.6 API Testing page:
   - Make real fetch calls to actual API endpoints with proper auth

3.7 Settings page:
   - Load/save provider profile to/from Firestore users collection

### SECTION 4: API ROUTES
4.1 POST /api/products - create product (implement with validation)
4.2 GET /api/subscriptions - list user's subscriptions (with product enrichment)
4.3 POST /api/subscriptions - acquire product (handle free/paid tiers)
4.4 GET /api/products - public product listing (with filtering/search)
4.5 Fix all other stub API routes (replace { ok: true } with real Firestore ops)

### SECTION 5: MAIN SITE PRODUCT LISTINGS
5.1 Products marketplace grid (`app/products/page.tsx`):
   - Fetch live products from Firestore instead of importing from lib/products.ts

5.2 Product detail page (`app/products/[slug]/page.tsx`):
   - Fetch product by slug or ID from Firestore with status check

5.3 "Acquire" button state:
   - Check subscription status on page load to show correct button state

### SECTION 6: MY APIS DASHBOARD (`app/dashboard/apis/page.tsx`)
   - Fetch real subscriptions on load
   - Show masked API keys with reveal/copy functionality
   - Display real usage bars (callsUsed/callsLimit)

### SECTION 7: DOMAIN ORDERING
7.1 Domain search:
   - Verify real RDAP/Name.com integration (per existing implementation)

7.2 Domain registration form:
   - Verify POST /api/domains/register writes to domainOrders collection

7.3 Domain dashboard (`app/domains/dashboard/page.tsx`):
   - Fetch real domains and pending orders from Firestore

### SECTION 8: NAVIGATION & AUTH STATE
8.1 Main nav auth state:
   - Use useAuth hook to show correct state (user menu vs sign in)

8.2 Protected route redirects:
   - Add auth checks to all protected pages with redirect to /auth

8.3 Sign in/Sign up pages:
   - Wire to Firebase Auth and create session cookies

### SECTION 9: FOUNDER ADMIN PANEL (`app/founder/page.tsx`)
9.1 Dashboard stats:
   - Query real counts from Firestore collections

9.2 Domain orders table:
   - Fetch real pending orders with user data

9.3 Fulfil modal:
   - Save ncOrderId and update order status
   - Create domain document upon fulfilment

9.4 API subscriptions table:
   - Fetch real subscriptions with enrichment

9.5 Users table:
   - Fetch real user data from Firestore

### SECTION 10: STATIC DATA AUDIT
Find and replace all instances of:
- Hardcoded product arrays → Firestore queries
- Hardcoded stats → calculated from real data
- Hardcoded chart data → Firestore time-series queries
- Fake user data → useAuth or server auth
- Empty form handlers → real API calls with loading/error states

### SECTION 11: FIRESTORE COLLECTIONS
Verify/initialize these collections:
- users (Firebase Auth UIDs)
- products (all products)
- subscriptions (user product subscriptions)
- apiKeys (hashed API keys)
- transactions (payment records)
- domainOrders (domain purchase requests)
- domains (fulfilled registrations)
- system (configuration)

Ensure required indexes exist in Firebase Console.

### SECTION 12: ERROR HANDLING & EMPTY STATES
For every page/component:
- Loading state: show skeleton/spinner during async ops
- Empty state: show proper UI when query returns 0 results
- Error state: show error message when operation fails
- Auth error: redirect to /auth if unauthenticated on protected pages

### SECTION 13: VERIFICATION CHECKLIST
Confirm each item works with real data:
- Auth: Google/GitHub/email sign in, session cookies, protected redirects
- Products: real data from Firestore, acquire buttons work correctly
- Console: real stats, products, analytics, earnings
- My APIs: real subscriptions, API key handling, usage tracking
- Domains: real search, orders, dashboard
- Founder Admin: real stats, tables, fulfilment
- General: no hardcoded data, proper loading/error/empty states

## IMPLEMENTATION APPROACH
We will implement these changes systematically, starting with Firebase configuration and authentication, then moving through each section of the application. We'll prioritize core functionality (auth, product listings, console) before moving to admin panels.

Each change will be tested to ensure it works with real Firebase data before proceeding to the next section.
